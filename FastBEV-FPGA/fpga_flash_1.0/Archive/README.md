# Archive

`lut_engine_legacy.v` 是目录整理前 `temp/lut_engine.v` 的历史版本。它保留用于
差异追溯，不属于 Flash v1.0 当前构建输入，也不在
`Vivado_Project/rtl/filelist.f` 中。

当前版本以 `Vivado_Project/rtl/lut_engine.v` 为准；不要将归档文件与现行 RTL
混合加入同一个 Vivado 工程。
